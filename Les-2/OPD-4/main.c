// Opdracht 4:
// We gaan nu met een tekstbestand werken. Zie H9.7
// In het tekstbestand staat uitslagen van een voetbalclub. Hier onder een voorbeeld. Bij een andere aantal regels (max 36) en uitslagen werkt je programma ook correct. Een regel is wel op de manier uit het voorbeeld opgebouwd:
// Thuis 1 - 0 PSV
// Uit 0 – 3 ADO
// Uit 2 – 0 Sparta

// a)	Gebruik een struct  om ieder regel uit het bestand op te slaan. 
// b)	Print nu alle tegenstanders
// c)	Vervolgens ga je op basis van de array van structs de punten en het doelsaldo bereken.
// d)	Sla ook het resultaat van deze berekening op in een andere struct. Print op basis van dit bestand: 3 wedstrijden, 6 punten en doelsaldo (4-2). NB: gelijkspelen kunnen uiteraard ook
// e)	Geef de gebruiker de mogelijkheid om de naam van een tegenstander in te typen en geef aan of van die club is gewonnen of niet. 

#include <stdio.h>
#include <string.h>

int main(){

    return 0;
}