void print_max(char* s) {
	int max_karakters = 20;
	for (int i = 0; i < strlen(s) && i < max_karakters; i++) {
		printf("%c", s[i]);
	}
	if (max_karakters < strlen(s)) {
		printf("... ");
	}
	printf("\n");
}
